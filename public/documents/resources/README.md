# EVADA Resources documents

Place approved PDFs and downloadable resources here.

Update the matching entry in `src/data/resources.ts`:

- `downloadUrl` for PDFs and whitepapers
- `href` for live documentation pages
- `assetStatus` to `available` only when the file or route exists

Do not point download buttons to files that have not been added yet.
